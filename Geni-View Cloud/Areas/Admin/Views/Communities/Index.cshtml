@model IEnumerable<GeniView.Data.Web.Community>
@{
    ViewBag.Title = "Communities";
}
@section scripts
{
    <script type="text/javascript">
        $(document).ready(function () {
            var oTable = $('#CommunitiesTable').DataTable({
                responsive: { details: true },
                dom: 'tip',
                pageLength: 13,
                columnDefs: [
                    { searchable: false, orderable: false, targets: [-1] },
                    { responsivePriority: 1, targets: -1 },
                    { responsivePriority: 10, targets: [2, 3, 4] }
                ],
            });

            // Wire custom search box
            $('#communitySearch').on('keyup', function () {
                oTable.search(this.value).draw();
            });
        });
    </script>
}

@await Html.PartialAsync("_CommunitiesListPartial")
